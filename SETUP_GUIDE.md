# Skill-Link MVP - Setup & User Guide

## 🎉 ฟีเจอร์ที่ใช้งานได้จริง

โปรเจค Skill-Link MVP ได้รับการปรับปรุงให้ใช้งานได้จริงแล้วด้วยฟีเจอร์ต่อไปนี้:

### ✅ ระบบยืนยันตัวตน (Authentication)
- **สมัครสมาชิก**: สร้างบัญชีใหม่ด้วยอีเมลและรหัสผ่าน
- **เข้าสู่ระบบ**: ล็อกอินด้วย Firebase Authentication
- **ออกจากระบบ**: ฟังก์ชันปลอดภัย
- **ป้องกันหน้า**: redirect อัตโนมัติหากไม่ได้ล็อกอิน

### ✅ ระบบโปรไฟล์
- **อัปโหลดรูปโปรไฟล์**: ใช้ Firebase Storage อัปโหลดรูปจริงได้
- **แก้ไขข้อมูลส่วนตัว**: ชื่อ, bio, ทักษะที่มี, ทักษะที่ต้องการเรียน
- **แสดงสถิติ**: จำนวนเซสชัน, คะแนน, ข้อมูลอื่นๆ
- **บันทึกถาวร**: ข้อมูลเก็บใน Firestore

### ✅ ระบบค้นหาผู้ใช้
- **ค้นหาจริง**: เชื่อมต่อ Firestore ค้นหาผู้ใช้จริง
- **กรองตามทักษะ**: ค้นหาจากทักษะที่มี, ต้องการเรียน, ชื่อ, bio
- **แสดงผลแบบเรียลไทม์**: แสดงรูปโปรไฟล์, สถานะออนไลน์
- **ระบบแนะนำ**: แสดง "Perfect Match" สำหรับทักษะที่ตรงกัน

### ✅ ความปลอดภัย
- **Firestore Rules**: ควบคุมการเข้าถึงข้อมูล
- **File Validation**: ตรวจสอบประเภทและขนาดไฟล์
- **Error Handling**: แสดงข้อผิดพลาดที่เข้าใจได้

## 🚀 วิธีการตั้งค่า Firebase

⚠️ **สำคัญมาก**: คุณต้องตั้งค่า Firebase Project จริงก่อนใช้งาน

### 🔥 หากเจอ Error: "api-key-not-valid"
👉 **อ่านไฟล์ `FIREBASE_SETUP.md` ก่อน** - มีขั้นตอนแก้ไขปัญหาละเอียด

### 1. สร้าง Firebase Project ใหม่
```bash
# ไปที่ https://console.firebase.google.com
# คลิก "Add project" และทำตามขั้นตอน
```

### 2. ตั้งค่า Authentication
```bash
# เข้าไป Authentication > Sign-in method
# เปิดใช้ Email/Password
```

### 3. ตั้งค่า Firestore
```javascript
// Firestore Security Rules (ใน Firebase Console)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only access their own document
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Allow authenticated users to read other users for search
    match /users/{userId} {
      allow read: if request.auth != null;
    }
  }
}
```

### 4. ตั้งค่า Storage
```javascript
// Storage Security Rules
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Users can upload profile photos
    match /profile-photos/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 5. เพิ่ม Environment Variables (⚠️ สำคัญมาก)
```bash
# คัดลอกไฟล์ .env.example เป็น .env
cp .env.example .env

# แก้ไขไฟล์ .env ด้วยค่าจริงจาก Firebase Console
REACT_APP_FIREBASE_API_KEY=AIzaSyC... # <- ต้องเป็นค่าจริง
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=123456789
REACT_APP_FIREBASE_APP_ID=1:123456789:web:abc123

# หลังจากแก้ไข .env แล้ว ต้องรีสตาร์ทเซิร์ฟเวอร์
npm start
```

### 🆘 แก้ไขปัญหาที่พบบ่อย
- **Error: api-key-not-valid** → อ่าน `FIREBASE_SETUP.md`
- **แอปไม่โหลด** → ตรวจสอบไฟล์ `.env` ว่าอยู่ในโฟลเดอร์รูท
- **ไม่สามารถสมัครได้** → เช็ค Firebase Console > Authentication ว่าเปิด Email/Password แล้วหรือยัง

## 🎯 วิธีการใช้งาน

### สำหรับผู้ใช้ใหม่:
1. **สมัครสมาชิค**: คลิก "Sign Up" > กรอกข้อมูล > ลงทะเบียน
2. **ตั้งค่าโปรไฟล์**: ไปหน้า Profile > แก้ไขข้อมูล > อัปโหลดรูป
3. **เพิ่มทักษะ**: เพิ่ม "ทักษะที่มี" และ "ทักษะที่ต้องการเรียน"
4. **ค้นหาผู้ใช้**: ไปหน้า Search > ค้นหาตามทักษะ
5. **เชื่อมต่อ**: กดปุ่ม "Connect" เพื่อขอเชื่อมต่อ

### สำหรับการพัฒนาต่อ:
1. **เพิ่มระบบแชท**: ใช้ Firestore Realtime ได้
2. **ระบบนัดหมาย**: เพิ่ม Calendar integration
3. **ระบบรีวิว**: เพิ่มการให้คะแนนและรีวิว
4. **การแจ้งเตือน**: ใช้ Firebase Cloud Messaging

## 🛠️ คำสั่งสำคัญ

```bash
# ติดตั้ง dependencies
npm install

# รันในโหมด development
npm start

# สร้าง production build
npm run build

# ติดตั้ง Firebase CLI (ถ้าต้องการ deploy)
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

## 📱 โครงสร้างแอป

```
src/
├── components/          # ส่วนประกอบ UI
├── context/
│   └── AuthContext.tsx  # จัดการการยืนยันตัวตน
├── pages/
│   ├── Login.tsx        # หน้าเข้าสู่ระบบ
│   ├── Register.tsx     # หน้าสมัครสมาชิก
│   ├── ProfilePage.tsx  # หน้าโปรไฟล์
│   └── SearchPage.tsx   # หน้าค้นหาผู้ใช้
├── config/
│   └── firebase.ts      # การตั้งค่า Firebase
└── App.tsx              # ส่วนหลักของแอป
```

## 🎉 สรุป

โปรเจค Skill-Link MVP นี้ได้รับการปรับปรุงให้:
- ✅ **ใช้งานได้จริง** ด้วย Firebase Backend
- ✅ **ระบบสมัคร/ล็อกอิน** ทำงานเต็มรูปแบบ
- ✅ **ค้นหาผู้ใช้** จากฐานข้อมูลจริง
- ✅ **อัปโหลดรูปโปรไฟล์** ได้จริง
- ✅ **ข้อมูลถาวร** ใน Firestore
- ✅ **ระบบความปลอดภัย** ครบถ้วน

สามารถใช้เป็นฐานพัฒนาต่อยอดได้เลย! 🚀